part of seven_star_utils;

class ConstantDimensions {
  static double deviceHeight = MediaQuery.of(SevenStarUtils.appContext!).size.height;
  static double deviceWidth = MediaQuery.of(SevenStarUtils.appContext!).size.width;
  static double pagePadding = 16;
}
